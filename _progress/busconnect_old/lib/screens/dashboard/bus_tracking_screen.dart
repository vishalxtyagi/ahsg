import 'package:flutter/material.dart';

// Track bus placeholder
class TrackBusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Track Bus'),
    );
  }
}